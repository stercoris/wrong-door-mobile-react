subscription SubsribeToNewMessages {
	newMessage {
		id
		userId
		message
		time
		deleted
	}
}

subscription SubsribeToDeletedMessages {
	deletedMessage {
		id
		userId
		message
		time
		deleted
	}
}
